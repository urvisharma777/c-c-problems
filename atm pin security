#include <stdio.h>
#include<string.h>

int main() {
    char correctpin[]="@5ab7&";
    char enteredpin[35];
    int i;
    for(i=0;i<3;i++){
        printf("Enter your pin: ");
        scanf("%s",enteredpin);
        if(strcmp(enteredpin,correctpin)==0){
            printf("Access granted\n");
            return 0;
        } else {
            printf("wrong pin\n");
        }
    }
    printf("card blocked\n");
    return 0;
}
